name: Ollama Deepseek MCP proxy
version: 0.0.1
schema: v1
mcpServers:
  - name: Ollama Deepseek proxy
    command: npm
    args:
      - run
      - mcp-server
    env:
      OLLAMA_HOST: "http://localhost:11434"
      OLLAMA_MODEL: "deepseek-coder"
      MCP_PORT: "3000"
